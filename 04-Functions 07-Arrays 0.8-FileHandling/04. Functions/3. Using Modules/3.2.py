###
# Generates and prints a random number between 1 and 6,
# similar to rolling a dice
#
import random

rand_int = random.randint(1,6)
print(f'Your random number between 1 and 6 is: {rand_int}')